[pytest]
testpaths = tests/hil
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (mock hardware)
    integration: Integration tests (can run on real hardware)
    slow: Tests that take significant time
addopts = -v --tb=short --strict-markers


